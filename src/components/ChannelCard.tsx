import React from "react";
import { Video } from "../utils/Model";

type Props = {
  video: Video;
};

const ChannelCard = ({ video }: Props) => {
  return <div>ChannelCard</div>;
};

export default ChannelCard;
